# Waymo active inference — papers, replication, surprise metrics, comfort-zone boundaries

Working repository for the active-inference driver-modeling line of work: reading the Waymo /
TU-Delft papers, replicating the collision-avoidance model, building a reusable library of
surprise measures, and developing a method for establishing driver **comfort-zone boundaries**.

## Start here

| Document | What it is |
|---|---|
| [`HANDOFF.md`](HANDOFF.md) | **Context reload after clearing a chat** — scope decisions, environment constraints, choices that were made rather than inherited, and what was deliberately not done. Nothing here is repeated from the notes |
| [`docs/software_overview.md`](docs/software_overview.md) | **The software in blocks** (2026-09-16) — the whole repository divided into six engines, three kinds of input and three kinds of output, one figure per block with the files behind each stage (`docs/make_software_overview_figures.py`), a table of where to go for a given question, and the floor of tests and rules. Written for jumping into the work; the handbook explains the model, this explains the software |
| [`notes/01_paper_summaries.md`](notes/01_paper_summaries.md) | One summary per paper (7 active-inference papers), with the mechanisms and parameter values worth remembering |
| [`notes/02_active_inference_overview.md`](notes/02_active_inference_overview.md) | The method itself: what active inference is, how it is instantiated for driving, what it buys over the alternatives, where it is weak, and how it connects to comfort zones. Includes the wider literature search |
| [`notes/03_replication.md`](notes/03_replication.md) | Replication report — what ran, what matched, what did not, and why |
| [`notes/04_comfort_zone_method.md`](notes/04_comfort_zone_method.md) | **The proposed comfort-zone method**, and the study that would validate it |
| [`notes/05_validation.md`](notes/05_validation.md) | Side-by-side comparison of our results against the published values — the collision-avoidance model *and* the surprise measures |
| [`docs/data_requirements.pdf`](docs/data_requirements.pdf) | **Data specification** — exactly what data is needed, in what format, and which datasets would serve. Built from `docs/data_requirements.md` with `docs/build_pdf.py` |
| [`docs/method_review.md`](docs/method_review.md) | **Review of the published method against its released code and data** (2026-08-23): replication status, three substantive findings on the reported results (undocumented road departures at 25 m/s; a surprise accumulator far from zero in benign following; worked examples that do not match the deposit), a 13-item table of code-versus-paper differences, methodological points, errata, and questions for the authors. Word and PDF generated from the markdown; deposit numbers regenerated by `replication/review_osf.py` |
| [`docs/crash_causation_plan.md`](docs/crash_causation_plan.md) | **Proposal (2026-08-23): crash causation inside the active inference driver**, rear-end only — the four Bärgman et al. (2024) mechanisms as switchable components around an active-inference response process, crashes generated from the QUADRIS seed scenarios, and tested for practical equivalence with the Wu et al. binning/ROPE framework. Reading notes on the three source papers (in `external/OtherNonActiveInference/`), component mapping, two execution tiers, protocol, architecture, inputs needed, work plan |
| [`docs/crash_causation_results.md`](docs/crash_causation_results.md) | **Results (final 2026-08-25): crash causation with an active-inference response, full QUADRIS population** — digitized input distributions, all 5 000 seeds, five causation components, two response processes. Headlines: the attentive active-inference driver avoids 67% of the crash population; its conditions are *closer* to the reference than the CBM control (severity θ 0.148 vs 0.209 with the abnormal component; P ≈ 0.97 as a difference under the settled uncertainty convention, corrected 2026-08-27 from an earlier overstated "every resample"); the tier-2 arbiter validates the timing surrogate (0.55 s median abs. difference, zero-start convention); the glance-gate finding (evidence gating vs inference gating). Figures by `replication/causation/make_results_figures.py` |
| [`docs/severity_vs_timing.md`](docs/severity_vs_timing.md) | **Why severity matches while timing and braking do not** (2026-08-26) — the dissociation (response timing changes how *many* crashes happen, not how *hard*: conditions B and C differ 4.7-fold in crash rate with severity quartiles agreeing to 0.3 m/s, 71% of impact-speed variance between-seed), plus the diagnosis that θ misreads three of the four metrics — t_nr lives on a 0.05 s lattice with 25 distinct reference values, and a_f,min carries a 48% atom at zero that collapses two quantile bin edges. Figures by `replication/causation/make_dissociation_figure.py` |
| [`docs/equivalence_rope_note.md`](docs/equivalence_rope_note.md) | **Setting the practical-equivalence ROPE** (2026-08-26) — a calibration of the Wu et al. framework and the project's settled configuration for it. Found that our original setup could not be passed by a model that is *exactly right*, traced it to three compounding choices (uniform bin weights, a defective weighted bootstrap, and treating the reference as a sample), and resolved it by settling that the QUADRIS 5 000 **are** the target population. Derives both thresholds from a stated tolerance on the injury-weighted mean (10% → θ 0.188, Θ 0.089) instead of inheriting them; shows that choosing the weight anchor P0 *is* choosing θthd; and shows that θ and Θ bound only between-bin allocation, so the aggregate must be reported separately. Written to be sendable to Jian Wu |
| [`docs/czb_study1_data_plan.md`](docs/czb_study1_data_plan.md) | **Using the Study 1/2 clip-rating and button-press data for comfort-zone boundaries** (2026-08-26) — what is in `external/01_studies/`, three data-quality findings not in its documentation, and a proposal: the button press *is* a timed CZB crossing rather than a proxy, the ego never responds so the stimulus is unconfounded, and the four scenarios with shared participants support the cross-scenario transfer test `docs/data_requirements.pdf` was written to request |
| [`docs/lane_entry_note.md`](docs/lane_entry_note.md) | **Making lane entry continuous** (2026-08-27) — the settled cut-in modification to the preference function: the binary lane gates become the lateral overlap fraction *predicted at longitudinal closure*, and the saturated safety magnitude becomes the counterfactual residual impact speed. Both parameter-free, both flag-gated (defaults = released behavior), both reducing exactly to the released forms in the geometries the released scenarios sustain; argued as the closed-form of the expectation the closed loop already computes by rollout. Restores TTC discrimination the binary forms could not express (`figures/cutin_field_check.png`, by `replication/czb/cutin_field_check.py`) |
| [`docs/czb_fitting_plan.md`](docs/czb_fitting_plan.md) | **Fitting plan for the CZB boundary on the study-1 data** (2026-08-27) — why the fit is a small mixed model rather than a high-dimensional search (everything upstream of the boundary is frozen; the field is precomputed; ~5–8 population parameters plus shrunk random effects), the three-stage model (static threshold → hierarchical ordered → accumulator, Button data held out), method recommendation (marginal Gauss-Hermite quadrature + Laplace on the hyperparameters — the plan's original "MAP + Laplace" was corrected by card A.1, which showed the joint mode inflates the between-driver spread 2.6-fold; synthetic-recovery first), and the stage-0 feasibility result: a two-parameter probit on the zero-fitted-parameter field reproduces the 18-cell human response surface with correlation 0.90 (`replication/czb/pilot_surface_fit.py`). Data pipeline: `src/comfortzone/czb_data.py` |
| [`replication/czb/out/`](replication/czb/out/) | **The stage-A fits and the query register** (regenerated 2026-08-29 as card A.2.v2) — synthetic recovery (`recovery_summary.md`), the stage-1 hierarchical fit of the boundary-level population (`stage1_summary.md`: median 5 400, between-driver σ 0.200 under the current specification — hierarchical lapse, k = 12, shown-window covariates; percentiles moved under 1% from the 2026-08-27 fit; the correlated-effects variant gives ρ = −0.717 against a −0.27 artifact baseline without moving the deliverable; comfort 3 418 / dread 6 572 from the ordered model), the bias-variant decision evidence (`bias_variant_diagnostics.md`), the accumulator verdict (`stage2_summary.md`: **FAIL**, final at review gate R.1, scope narrowed 2026-08-29 to the specified architecture), the C1 covariate defect and its fix (`c1_covariate_defect.md`), the Button-side check (`button_validation_summary.md`), and `query_register.md`, generated from `worklog.md` by `collect_queries.py` |
| [`docs/r2_gate_decisions.md`](docs/r2_gate_decisions.md) | **Review gate R.2: the decisions** (2026-08-29) — the pre-registered field-versus-gap comparison on the second cut-in study **rules the field's kinematic content out** on its own scenario (held-out wRMSE 0.347 against the log-gap threshold's 0.152, chance 0.320, noise floor 0.118; `out/cutin2_field_vs_gap.md`); the C1 inversion closed as a covariate-window defect; the expected-deficit sign derivation (`out/expected_deficit_sign.md`) fails the distance mechanism, so no lateral term is grafted; and the project headline is restated around the one-scalar **trait** claim (~69% of reliable per-driver signal shared across scenarios) with the criticality axis an open question that gap currently leads |
| [`docs/active_inference_scope_map.md`](docs/active_inference_scope_map.md) | **Scope map: what we use of active inference, what we do not, and what the method actually is** (2026-08-29) — an overview written after the R.2 rulings, with six diagrams (`docs/make_ai_scope_figures.py`). The used slice is three components (the inherited preference distribution, residual information, and the accumulator that R.1 falsified); planning, epistemic value and belief updating are switched off in the deliverable but run in the crash-causation closed loop. Sets out the dual-role claim and both of its failures, what survived (the one-scalar trait), and the paradigms the surviving method is actually made of — psychophysics, hierarchical Bayes, held-out comparison, comfort-zone boundary theory — with the R2.Q5 crux that every surprise measure needs a reference distribution |
| [`docs/r2_pipeline_review.md`](docs/r2_pipeline_review.md) | **Adversarial review of the R.2 decisive pipeline** (2026-09-02) — the verdict reproduced by an independent grid fit (cell-bootstrap interval on the field-minus-gap difference +0.17 to +0.22 against a 0.01 margin), and the field's loss decomposed: ~44% the project's lane-entry gate (suppresses the deficit in the slow-lane-change cells where participants respond most and are flat across lane-change duration), ~56% the released counterfactual magnitude (orders matched-TTC cells by absolute speed, not gap). One sign slip in the gate record's gloss corrected. Diagnostic `replication/czb/cutin2_lane_gate_diagnostic.py` |
| [`docs/surprise_without_the_field.md`](docs/surprise_without_the_field.md) | **Surprise without the field** (2026-09-02, the R2.Q5 design note) — the surprise family is an operator on a reference and with a free reference reproduces any threshold model, so scenario-agnosticism belongs to the reference; only two qualify: a predictive model of other agents (zero by construction on the cyclist overtake, so not the axis, but a pace-independent stimulus onset) and a population distribution of accepted states, which is the CZB ellipse (residual information under a Gaussian is half the squared Mahalanobis distance). Proposed framing: surprise defines the onset, a population percentile the level. Card Q5.1 (no preference code) in `docs/czb_work_orders.md` |
| [`docs/czb_ellipse_design_note.md`](docs/czb_ellipse_design_note.md) | **The CZB ellipse: a design note** (2026-09-02) — Jonas's joint-percentile ellipse read against what the data can identify: the study stimuli give the ellipse's shape (the trade-off between two observables, where a design varies both: study 2, LTAP, CAMP) and the per-driver ordering, but not its scale, which is confounded with the level until a population reference from naturalistic data pins it; observables per scenario; the per-driver trait mapped on by substitution into the stage-1 estimator; the identity with residual-information surprise; cards EL.1–EL.3 |
| [`replication/czb/out/stage1_looming.md`](replication/czb/out/stage1_looming.md) | **Card G1.Q1: stage 1 re-fitted on the gated looming rule** (2026-09-02, pre-registered in `replication/czb/fit_stage1_looming.py`) — the hierarchical per-driver level on log(optical expansion rate) with the study-2 gate fixed: LOPO log-likelihood +14.9 over the deficit axis on the identical code path, C1 predicted to 0.030; median level 0.032 rad/s (1.8°/s), 80th percentile 0.066 rad/s; one 5-point percentile step moves the implied onset 0.24 s against 0.52 s from the level's own CI. **Decision: the gated looming axis replaces the deficit axis as the stage-1 primary.** The gate is worth +111 LOPO units on this axis |
| [`replication/czb/out/ltap_two_axis.md`](replication/czb/out/ltap_two_axis.md) | **Card B.3.v2: the left turn across path, one axis or two** (2026-09-02, pre-registered in `replication/czb/ltap_two_axis.py`; loader `src/comfortzone/ltap.py`, 62 property checks in `tests/test_ltap.py`) — 18 cells, leave-one-PET-level-out: distance 0.056, arrival-time separation 0.112, linear 2D rule 0.054, quadratic form 0.062, the oncoming's looming rate 0.054; chance 0.288, noise floor 0.033. distance minus linear = +0.0018; distance minus quadratic = -0.0067; linear minus quadratic = -0.0085; distance minus looming = +0.0018. **neither 2D model beats distance alone by 0.01: one axis suffices on this data**; -; **looming and distance are indistinguishable (within 0.01)**. Bootstrap: distance minus linear mean +0.0019, 95% -0.0191 to +0.0282 (52% of resamples favour the linear rule); distance minus looming mean +0.0010, 95% -0.0219 to +0.0239 (50% of resamples favour looming). Query B3.Q1 resolved by assumption (decision moment 13.5 s) |
| [`replication/czb/out/cutin2_looming.md`](replication/czb/out/cutin2_looming.md) | **Card EL.1b: the cut-in axis is the looming rate** (2026-09-02, pre-registered in `replication/czb/cutin2_looming.py`) — a 1D threshold on the optical expansion rate (width 1.88 m from the traces) scores held-out wRMSE 0.113 against the EL.1 equal-weight rule's 0.114, gap 0.152, inverse tau (1/TTC) 0.168, optical size 0.152; the identity gap × TTC = width / expansion rate holds numerically; threshold 0.034 rad/s (1.9°/s) |
| [`replication/czb/out/cutin2_gate.md`](replication/czb/out/cutin2_gate.md) | **Card G.1: an anticipatory gate in front of the rule** (2026-09-02, pre-registered in `replication/czb/cutin2_gate.py`) — a fixed-horizon (3 s) lateral-clearance gate, fitted on post-onset cells only, predicts the 90 pre-onset cells out of sample at 0.032 (observed mean 0.023; the ungated rule 0.483) and improves the post-onset fit to 0.103 (from 0.114); m_lat 0.15 m, s_l 0.99 m; the gate is credited |
| [`replication/czb/out/cutin2_field_horizon_gate.md`](replication/czb/out/cutin2_field_horizon_gate.md) | **Card F.1: the field with a fixed-horizon lane-entry gate** (2026-09-02, attribution experiment; flag `lane_entry_horizon_s`, default off, in `src/aidriver/preferences.py`) — the field's held-out wRMSE goes from 0.347 to 0.261, the first field variant to beat chance, so the project's closure-time gate is confirmed as its share of the loss; the field remains ruled against (gap 0.152), and its within-row ordering does not improve (7 of 24 rows) |
| [`docs/handbook/17_appendix_test_track.md`](docs/handbook/17_appendix_test_track.md) | **Handbook appendix 17: the test-track anchor** (2026-09-02, early results) — the video paradigm against real left turns on the same model: boundary 2.18 s (video) against 2.45 s (track), within the design resolution; sharpness four times higher in the car (within-driver spread 0.20 s against 0.86 s); the video-fitted population carries over to the track's cells with nothing refitted (0.200 against the track's own 0.231, chance 0.289); held-out drivers predicted at +0.20 log-likelihood per run over chance; the graded rating's slope matches. Axis and gate untested there (one speed, fixed decision moment); the dread boundary parked |
| [`replication/czb/out/ltapod_testtrack.md`](replication/czb/out/ltapod_testtrack.md) | **Card TT.1: the 2013 LTAP/OD test-track study against the video LTAP study** (2026-09-02, pre-registered in `replication/czb/ltapod_testtrack.py`) — the same hierarchical threshold model on real turns (26 drivers, 218 comfort runs against SetPET) and on the video's 50 km/h cells (43 drivers): median comfort boundary 2.45 s on the track, 2.18 s on video, video minus track −0.27 s (SE 0.20), inside the 0.5 s design resolution — **the video paradigm reproduces the test-track comfort boundary**; drivers are sharper on the track (within-driver spread 0.20 s against 0.86 s). The paper's own bracket statistics reproduce from the protocol (2.17 / 1.49 s, ratio 0.68 against 2.26 / 1.50, 0.69). With every run rated, the comfort rating's slope against PET matches the video's perceived-safety slope (−0.31 against −0.40 z per s). The hurried condition's SetPET fit is not trusted (drivers accelerated; the model-free bracket midpoint is 0.80 s) |
| [`notes/06_bargman2015_ltapod_testtrack.md`](notes/06_bargman2015_ltapod_testtrack.md) | **Bärgman, Smith & Werneke (2015): comfort- and dread-zone boundaries in LTAP/OD on a test track** — summary of the paper (median comfort boundary PET 2.26 s, dread 1.50 s, hurried ≈ 0.69 × comfortable) and what its run protocol (`external/02_LTAPOD_DBIN/`, 2013, real turns against a balloon car at 50 km/h) gives this project: the video-to-driving offset on one scenario, a second between-driver spread, a dread boundary, and a graded rating against PET |
| [`docs/handbook/13_glossary.md`](docs/handbook/13_glossary.md) | **Handbook chapter 13, now with the measurement vocabulary** (2026-09-02, {{R6}}) — twenty-one terms of the comfort-zone measurement program in plain words with their numbers and files: comfort-zone boundary, axis, optical expansion rate, deficit, level, gate, trait, percentile, cell, held out, noise floor, chance, pre-registered, card, the review gates, timepoint, lapse, LOPO, reliability ceiling, surprise (in the program), the ellipse. Paired with the concepts deck (`presentation/talk/build_concepts_talk.py`), one animated slide per term |
| [`docs/handbook/16_appendix_external_rh_model.md`](docs/handbook/16_appendix_external_rh_model.md) | **Handbook appendix 16: an external comfort-zone model built on required deceleration** (2026-09-02, early work, external) — a colleague's habitual-control-resolvability model and another LLM's fit of it to the second cut-in study, read against this project's results: both pipelines find gap first with a speed exponent of about 0.4-0.5, both reject physics-first (deceleration) cues for the same reason, and both find a reliable per-driver criterion; their anticipatory gate helps where ours hurt. One cross-check on our registered pipeline (`replication/czb/cutin2_external_cue.py`). `OthersWork/` is gitignored and is not standing context |
| [`docs/ltap_construction_note.md`](docs/ltap_construction_note.md) | **The left-turn-across-path scenario: construction note** (2026-09-02, card B.3.v2) — the 18 traces measured (`replication/czb/ltap_geometry.py`): the ego's turn script is identical in all, so at matched PET the oncoming's time-to-arrival is matched and its distance differs by 1.4×; the Random responses fit a distance reading to RMS 0.08 with a residual speed effect (the 70 km/h cells intervene 0.06 less), where a time-only reading errs by −0.20. Comparator-class construction (roles by yaw span, arrival-time separation and oncoming distance at the decision moment, pre-stated 1D/2D comparison); the field alternative documented in §3 and not built |
| [`handover_2026-09-03.md`](handover_2026-09-03.md) | **The 2026-09-02 → 09-03 arc** — the model became gate × axis × level (looming axis, anticipatory gate, stage 1 re-fitted), the left turn's axis, the test-track anchor, the concepts deck and vocabulary, the external appendix; `handover.md` rewritten the same night as the entry point for a less capable model |
| [`handover_2026-09-02.md`](handover_2026-09-02.md) | **The 2026-09-01 → 09-02 arc** — the decks, the pipeline review, three design notes, card EL.1, and the pitfalls found; `handover.md` was rewritten the same day as the entry point for a less capable model |
| [`replication/czb/out/cutin2_two_axis.md`](replication/czb/out/cutin2_two_axis.md) | **Card EL.1: a second axis earns its place on the second cut-in study** (2026-09-02, pre-registered in `replication/czb/cutin2_two_axis.py`) — an equally weighted linear rule in log gap and log TTC scores held-out wRMSE 0.114 against the 1D log gap's 0.152, at the sampling-noise floor; the quadratic form 0.115, so parsimony keeps the linear rule: the cut-in's criticality axis is a threshold on the geometric mean of gap and TTC |
| [`docs/overtake_construction_note.md`](docs/overtake_construction_note.md) | **The cyclist-overtake field, and what building it revealed** (2026-08-28) — the second scenario's loader and the two conventions that had to be established from the data (the lateral coordinate is `Location_Y`; `Offset` inverts the criticality ordering), the finding that the rear-end field does *not* carry over (it orders the 15 cells at Spearman +0.402 against the clearance label's −0.833, because `p_lane` saturates at 0.843–1.000), the S-shaped lane-entry ramp (direction supported, optimum k ≈ 12, gain 2.9%), the transfer result (frozen 0.176 vs chance 0.158; freeing only the per-scenario lapse 0.129, within 0.016 of the refit ceiling), and the field-independent bound: ~69% of the reliable per-driver signal is shared across all four scenarios |
| [`docs/lateral_and_uncertainty_note.md`](docs/lateral_and_uncertainty_note.md) | **Perceptual uncertainty, the distance–time anomaly, and the missing lateral term** (2026-08-28) — why two findings the field cannot explain (LTAP intervention *lower* at 70 km/h than 50 at every matched PET; a collision-free cyclist pass at 0.5 m being uncomfortable) are one problem: the field evaluates the deficit along a single predicted trajectory, so it has no probability mass off that path and no dispersion that grows with distance. Reviews five papers (Zgonnikov/Abbink/Markkula 2024; Wang et al. 2024 on bounded-optimal decisions under noisy vision; Mohammad et al.; the Bontje et al. 2026 evidence-accumulation review; Kolekar et al. 2020's Driver's Risk Field) and proposes taking the deficit in expectation over a predictive distribution — a return to the framework's own form rather than a bolt-on. Design decisions handed to review gate R.2 |
| [`replication/czb/out/cutin2_scope.md`](replication/czb/out/cutin2_scope.md) | **What the second cut-in study settles** (2026-08-28) — the study-1 cut-in design has constant relative speed, so correlation(gap, TTC) = 1.0000 and its longitudinal fits cannot distinguish the preference field from a gap threshold. The new study breaks that over a six-fold range of gap: all 24 matched-TTC rows run negative, gap orders the response at ρ −0.887 against TTC's −0.807, and required deceleration manages +0.238. Also the clean exposure test study 1 lacks (P(intervene) 0.547 → 0.575 on the second showing of an identical clip) |
| [`docs/handbook/`](docs/handbook/) | **The understanding pack** — a 15-chapter handbook explaining the model for a mixed audience (math-free main text, layered notes): lineage and the debate, a real-data walkthrough, the machinery, scenario switching, norms, crash causation, modification recipes, the CZB path, code map, glossary, deep-end appendix. Markdown source; Word (for commenting) and PDF generated by `docs/handbook/build_handbook.py` |

## Layout

```
papers/active-inference/    the 7 papers + the Nature Communications Supplementary Information
notes/                      summaries, method overview, replication report, CZB method
notes/paper_text/           extracted plain text of every paper (for searching)
external/aica/              the authors' code, cloned (one patch; see replication/PATCHES.md)
external/gs4bu-osfstorage-archive/  the paper's OSF deposit: per-run simulation output for all
                            three scenarios (setups, outcome analysis, per-timestep pickles)
replication/               single-scenario runner + analysis for the authors' model;
                            osf/ holds the distribution-level comparison against the OSF
                            deposit (validate_osf.py; notes/05 §4b has the findings)
src/surprise/               library of surprise measures
src/aidriver/               independent NumPy re-implementation of the model
src/comfortzone/            comfort-zone field, boundaries, calibration
src/quadris/                QUADRIS rear-end seed scenarios: loading, sampling, metrics
src/causation/              crash-causation components (glances, deceleration cap, no-response)
                            around two response models (tier-1 active inference; the CBM of
                            Bärgman et al. 2024); see docs/crash_causation_plan.md
src/equivalence/            REUSABLE practical-equivalence testing (Wu et al. 2026 binning/ROPE)
replication/causation/      accumulator calibration on the OSF deposit + the QUADRIS runner
replication/czb/            cut-in field validation + the stage-0 surface fit (study-1 data)
tests/                      property tests (31 + 33 + 40 + 87 = 191, all passing)
figures/                    generated figures
demo_surprise.py            surprise measures on a cut-in; the AI agent on a rear-end conflict
demo_comfort_zone.py        comfort-zone fields and boundaries
docs/                       data specification (markdown source + generated PDF) and its builder
presentation/               45-minute talk (Chalmers template, speaker notes), generated by
                            presentation/build_deck.py
tools/czb_explorer/         interactive explorer suite: boundary explorer, norm-tournament
                            sandbox, and OSF model browser (static pages, verified math;
                            Dockerfile included)

```

Documents are written in markdown and the PDF is generated from it:

```bash
python docs/build_pdf.py docs/data_requirements.md
```

## Quick start

```bash
python tests/test_surprise.py         # 31 property tests
python tests/test_comfortzone.py      # 33 property tests
python tests/test_causation.py        # 40 property tests (causation + equivalence)
python tests/test_cutin.py            # 87 property tests (lane entry, cut-in, overtake, covariate window)
python demo_comfort_zone.py           # comfort-zone figures + critical-THW table
python demo_surprise.py               # surprise measures + active-inference agent
```

Requires numpy, scipy, matplotlib. The authors' code additionally needs torch, pandas,
seaborn, networkx, openpyxl (all installed here; torch is CPU-only).

## What works, and how well

**Surprise library** (`src/surprise/`) — solid. Implements the three families from
Modirshanechi et al.'s taxonomy plus the two measures introduced by Dinparastdjadid et al.:

- probabilistic mismatch: surprisal, **residual information**, Macedo's S8, Bayes-factor
  surprise, state prediction error
- belief mismatch: Bayesian surprise, **antithesis**, postdictive, confidence-corrected
- observation mismatch: absolute / squared error, unsigned RPE
- preference-relative: pragmatic value, residual information of pragmatic value, and the
  evidence accumulator that turns it into response timing

Works on Gaussians, Gaussian mixtures (trajectory-predictor output), particle sets
(active-inference beliefs) and categorical distributions through a common interface. The
tests verify the *claims*, not just that the code runs: the zero-floor property, invariance to
discretization, the Modirshanechi equivalences (`S_SPE = 1 − exp(−S_Sh)`), and that antithesis
silences mode-narrowing and mode-removal while firing on genuine surprise.

**Replication of the authors' model** (`external/aica` + `replication/`) — runs; two conditions
tested. At the paper's **Fig. 3b** case (v0 = 25 m/s, 1.0 s gap) it **reproduces the published
result**: response time 0.80 s against ~0.6–0.8 s, with the correct brake-and-swerve maneuver.
At **Fig. 3a** (v0 = 15 m/s, 1.5 s gap) it gives 0.80–1.04 s and swerves in 3 of 4 runs; the
paper's worked example for that condition shows 1.4 s and brake-only, but the authors' OSF
deposit for the same condition shows 0.8 s (31 of 32 seeds) and a lane change in 78% — so
Track A matches the authors' model, and the paper's example does not represent their data
(`docs/method_review.md` §3.1). Cost: ~18 s of CPU per simulated step.

**Independent re-implementation** (`src/aidriver/`) — mechanisms and preference function
faithful (verified against the Supplementary Information). Run over the paper's full
28-condition sweep (140 runs), it **reproduces 2 of the 6 published relations**: maneuver
choice versus speed, and braking magnitude versus urgency — the two that rest on the
preference function. The four that fail all depend on evidence-accumulation timing, where
response times are wildly dispersed (median 0.20 s, sd 1.23 s) and the collision rate is 34%
against the paper's "only at the shortest time gap". Do not use it for response-timing claims
yet. `notes/05_validation.md` has the comparison; `notes/03_replication.md` the diagnosis.

**Comfort-zone method** (`src/comfortzone/`) — this is the part built to be used. It depends
only on the verified pieces (the preference function and residual information), not on the
closed-loop controller. Gives:

- a scalar comfort-zone field over any 2-D slice of the state space, decomposable into the six
  preference terms so exceedances can be attributed to a cause;
- a **closed-form** comfort-zone boundary for car following, cross-checked against the numeric
  level set to 0.000 m;
- comfort vs dread boundaries as a single interpretable choice of allowed deceleration;
- "extra motives" as parameter changes with quantified effects;
- calibration of the boundary level against observed response onsets, from recorded kinematics
  alone — no model roll-out required, which is what makes it applicable to naturalistic data.

Sample output (steady following, lead assumed capable of −6 m/s², 1 s response time):

| speed | THW* at 8 m/s² (dread) | at 4 m/s² (comfort) |
|---|---|---|
| 10 m/s | 0.85 s | 1.48 s |
| 15 m/s | 0.73 s | 1.67 s |
| 20 m/s | 0.61 s | 1.86 s |
| 30 m/s | 0.40 s | 2.27 s |

## Important caveat

No human data has been used anywhere in this repository. Every number is a property of the
model. The comfort-zone method is a construction, not yet a finding — §7 of
`notes/04_comfort_zone_method.md` sets out the cross-scenario generalization test that would
turn it into one.

## Sources

Papers are indexed in [`papers/README.md`](papers/README.md) (54 from
<https://waymo.com/safety/research/>, 41 downloadable). The model replicated here is
Schumann, Engström, Johnson, O'Kelly, Messias, Kober & Zgonnikov, *Active inference as a model
of collision avoidance behavior in human drivers*, Nature Communications 17:5009 (2026),
doi [10.1038/s41467-026-73345-0](https://doi.org/10.1038/s41467-026-73345-0); code at
<https://github.com/tud-hri/Active-Inference-Collision-Avoidance> under a non-commercial
license permitting research use and benchmarking.
