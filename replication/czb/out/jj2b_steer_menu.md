# Card JJ.2b -- card JJ.2 rerun with steering, and with the released planner's own policy space

Generated by `replication/czb/jj2b_steer_menu.py`. **Post-hoc, and NOT a re-decision**: card JJ.2's verdict (`out/jj2_rollout_cutin.md`, DROP by rule (a)) was reached under the rules design note section 2 fixed in advance and stands as registered. This card is the reproduction Jonas asked for after card RE.1 found that the premise of JJ.2's menu was wrong. Do not edit by hand.

Card RE.1 part C: on this very design the released CEM planner's chosen escape is a steer in 36 of 36 cells and never a brake. Ruling JJ1.Q1's no-steering constraint is right for the RESPONSE side and wrong for the MODEL side of a policy comparison, so steering goes back on the model side only.

## 1 The three constructions on card JJ.2's own cells, folds and metric

| construction | post-onset held out | pre-onset out of sample | matched-TTC rows | rho(axis, share) | rho(axis, gap) | cells at Delta G = 0 |
|---|---|---|---|---|---|---|
| Delta G, JJ.1 menu (reproduces `out/jj2_rollout_cutin.md`) | 0.3202 | 0.5381 | 0 of 24 | -0.648 | +0.848 | 0 |
| Delta G, **menu + steering** | 0.3202 | 0.5381 | 0 of 24 | -0.330 | +0.635 | 0 |
| Delta G, **the released planner, no menu** | 0.3161 | 0.5538 | 19 of 24 | +0.169 | -0.308 | 0 |
| eps = G(continue), the model's own Eq. 13 signal | 0.3209 | 0.5355 | 3 of 24 | -0.195 | +0.332 | 0 |

Comparators on file: the gated looming rule 0.1027, the ungated 0.1137, card G.1's pre-onset 0.0319, chance 0.320, the sampling-noise floor 0.118. The response is ordered by the gap at -0.862, so an axis that is criticality must show rho(axis, gap) NEGATIVE.

## 2 What each construction picks as the best alternative

| policy | JJ.1 menu | menu + steering |
|---|---|---|
| continue | 0 | 0 |
| ease_off | 52 | 37 |
| brake | 259 | 32 |
| brake_hard | 67 | 0 |
| steer | 0 | 301 |
| swerve | 0 | 8 |

The released planner steers in **378 of 378** cells and brakes in **0**; its median first acceleration is +0.35 m/s^2 and its median max |omega| is 0.211 rad/s. That reproduces card RE.1 part C on the study's real traces rather than on its design grid.

With steering available the menu's own best alternative is a steer in **309 of 378** cells, so the two constructions agree about what the model would do.

## 3 The reading, as pre-stated and with no verdict attached

The pre-stated reading was: if adding the policies the model would actually choose moves the held-out score toward the gated looming rule, the menu was the binding defect; if not, the defect is the one card RE.1 located. **The answer is that the two halves of that sentence come apart, and both halves have to be reported.**

**The score does not move.** Post-onset held out goes 0.3202 (JJ.1 menu) -> 0.3202 (menu + steering) -> 0.3161 (the released planner), against the gated looming rule's 0.1027 + 0.01 and chance 0.320. On the pre-stated criterion the menu was **not** the binding defect: no construction here comes near the comparator, and every one of them sits at chance.

**The DIRECTION does move, and a long way.** The matched-TTC rows ordered the way the participants order them go from **0 of 24** with the JJ.1 menu to **19 of 24** with the released planner, and rho(axis, gap) turns from +0.848 to -0.308 -- from the wrong sign to the right one. Menu + steering sits between the two (+0.635, 0 of 24). So giving the model its own policy space does repair the inversion that gate R.2, card JJ.2 and card JJ.3 all failed on; what it does not do is turn the repaired ordering into a usable axis, because rho(axis, share) only reaches +0.169.

Two readings follow, and both are readings rather than decisions.

1. **The no-steering menu was a real defect and card RE.1 found it correctly.** A policy comparison that excludes the model's own chosen action inverts the ordering; with steering restored, 19 of 24 matched-TTC rows come out the human way. That is the largest movement on this design any card has produced, and it is evidence that the framing matters, not only the constants.
2. **And it is still not an axis.** The ordering is right-signed and weak, so the three-parameter threshold model recovers nothing beyond the training mean. What limits the magnitude is what card RE.2 measures directly: on these stimuli eps is the collision factor plus the safety factor and nothing else, and the safety factor is anti-correlated with the response at rho -0.861.

Run time 37 s.
